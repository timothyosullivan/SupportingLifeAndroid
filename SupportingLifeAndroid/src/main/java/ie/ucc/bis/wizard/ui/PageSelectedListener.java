package ie.ucc.bis.wizard.ui;

/**
 * Interface: PageSelectedListener
 * 
 * Listener interface to indicate the current
 * page selected as per the bread-crumb visual indicator
 * 
 */
public interface PageSelectedListener {
	void onPageStripSelected(int position);
}
