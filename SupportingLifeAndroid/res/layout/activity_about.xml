<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="fill_parent"
    android:layout_height="fill_parent"
    android:orientation="vertical" >

    <LinearLayout style="@style/TitleBar" >

        <ImageButton
            style="@style/TitleBarAction"
            android:contentDescription="@string/description_home"
            android:onClick="onClickHome"
            android:src="@drawable/title_home" />

        <ImageView
            style="@style/TitleBarSeparator"
            android:contentDescription="@string/title_bar_separator_description" />

        <TextView style="@style/TitleBarText" />

        <ImageButton
            style="@style/TitleBarAction"
            android:contentDescription="@string/description_about"
            android:onClick="onClickAbout"
            android:src="@drawable/title_about" />

        <ImageButton
            style="@style/TitleBarAction"
            android:contentDescription="@string/description_search"
            android:onClick="onClickSearch"
            android:src="@drawable/title_search" />
    </LinearLayout>

    <ImageView
        android:id="@+id/supporting_life_tree_image"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginBottom="10dp"
        android:layout_marginTop="10dp"
        android:background="@color/background1"
        android:contentDescription="@string/supporting_life_tree_image"
        android:src="@drawable/supporting_life_tree" />

    <ImageView
        android:id="@+id/spinning_wheel_image"
        android:layout_width="match_parent"
        android:layout_height="200dp"
        android:layout_gravity="fill_vertical"
        android:layout_marginBottom="15dp"
        android:layout_marginTop="10dp"
        android:visibility="invisible"
        android:contentDescription="@string/spinning_wheel_image"/>
    
    <!-- Notes:
    	android:fadeScrollbars="false" : keeps scrollbar visible on view
    -->
    <TextView
        android:id="@+id/about_text_content"
        style="@style/AppTheme"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="fill_horizontal"
        android:layout_marginBottom="15dp"
        android:background="@color/title_background"
        android:gravity="center_horizontal|fill_horizontal"
        android:maxLines="15"
        android:scrollbarAlwaysDrawVerticalTrack="true"
        android:scrollbarStyle="insideInset"
        android:fadeScrollbars="false"
        android:scrollbars="vertical"
        android:text="@string/about_intro"
        android:textColor="@color/title_text" />

</LinearLayout>